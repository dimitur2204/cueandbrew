package via.dk.cueandbrew.viewmodel;

import via.dk.cueandbrew.model.CreateReservationModel;

public class CreateReservationViewModel {
    private final CreateReservationModel createReservationModel;
    public CreateReservationViewModel(CreateReservationModel createReservationModel) {
        this.createReservationModel = createReservationModel;
    }
}
